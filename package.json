{
  "name": "@waveshq/standard",
  "version": "0.0.0",
  "private": true,
  "files": [],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "format": "prettier --write .",
    "lint": "turbo run lint -- --fix",
    "prepare": "husky install",
    "test": "turbo run test",
    "write": "turbo run write"
  },
  "lint-staged": {
    "*": "prettier --write --ignore-unknown"
  },
  "prettier": "@waveshq/standard-prettier",
  "devDependencies": {
    "@birthdayresearch/sticky-prettier": "^0.5.14",
    "@birthdayresearch/sticky-turbo": "^0.5.14",
    "@waveshq/standard-prettier": "workspace:*"
  },
  "engines": {
    "node": "^18.0.0 || ^16.0.0",
    "pnpm": ">=8.5.0"
  }
}
